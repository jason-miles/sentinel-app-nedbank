# The main pipeline for fraud_aml_pipeline

resources:
  pipelines:
    fraud_aml_pipeline_etl:
      name: nedbank_fraud_aml_pipeline_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/fraud_aml_pipeline_etl"

      libraries:
        - glob:
            include: ../src/fraud_aml_pipeline_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
